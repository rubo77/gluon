{
    hostname_prefix = 'Gib mir einen Namen ',
    site_name = 'Freifunk Travis',
    site_code = 'fftrv',

    opkg = {
        openwrt = 'http://opkg.services.fftrv/%n/%v/%S/packages',
        extra = {
            modules = 'http://opkg.services.fftrv/modules/gluon-%GS-%GR/%S',
        }, 
    },

    prefix4 = '10.116.128.0/17',
    prefix6 = 'fda1:384a:74de:4242::/64',

    timezone = 'CET-1CEST,M3.5.0,M10.5.0/3', -- Europe/Berlin
    ntp_servers = {'1.ntp.services.fftrv'},
    
    -- Wireless regulatory domain of your community.
    regdom = 'DE',

    config_mode = {
        -- Show/hide the altitude field
        geo_location = {
            show_altitude = true,
        },
        -- define if the contact field is obligatory (optional)
        owner = {
            obligatory = true
        },
    },

    -- Wireless configuration for 2.4 GHz interfaces.
    wifi24 = {
        channel = 11,
        
        -- List of supported wifi rates (optional)
        -- without 802.11b compatibility for better performance
        supported_rates = {6000, 9000, 12000, 18000, 24000, 36000, 48000, 54000},
        
        -- List of basic wifi rates (optional, required if supported_rates is set)
        -- without 802.11b compatibility for better performance
        basic_rate = {6000, 9000, 18000, 36000, 54000},

        -- ESSID used for client network.
        ap = {
            ssid = 'http://travis.freifunk.net/',
            -- disabled = true, (optional)
        },
        mesh = {
            id = '16:ca:ff:ee:ba:be',
            mcast_rate = 12000,
        },
    },
    
    -- Wireless configuration for 5 GHz interfaces.
    -- This should be equal to the 2.4 GHz variant, except
    -- for channel.
    wifi5 = {
        channel = 44,
        ap = {
            ssid = 'http://travis.freifunk.net/',
        },
        mesh = {
            id = '16:ca:ff:ee:ba:be',
            mcast_rate = 12000,
        },
    },

    -- The next node feature allows clients to always reach the node it is
    -- connected to using a known IP address.
    next_node = {
        -- anycast IPs of all nodes
        ip4 = '10.116.254.254',
        ip6 = 'fda1:384a:74de:4242::ffff',

        -- anycast MAC of all nodes
        mac = '36:f4:54:fc:e5:11'
    },

    fastd_mesh_vpn = {
        -- List of crypto-methods to use.
        methods = {'salsa2012+umac'},
        -- configurable = true,
        enabled = true,

        mtu = 1280,
        groups = {
            backbone = {
                -- Limit number of connected peers to reduce bandwidth.
                limit = 1,
                peers = {
                    fftrv_vpn0 = {
                        key = 'ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff',
                        remotes = {
                            'ipv6 "vpn0.travis.freifunk.net" port 11235',
                            'ipv4 "vpn0.travis.freifunk.net" port 11235'
                        },
                    },
                },
            },
        },
    },

    bandwidth_limit = {
        enabled = false,
        egress = 800,
        ingress = 4000,
    },

    autoupdater = {
        -- Default branch. Don't forget to set GLUON_BRANCH when building!
        branch = 'stable',
        
        branches = {
            -- stable releases are only rolled out selectively after a lot of testing in the rc branch
            stable = {
                name = 'stable',
                mirrors = {
                    'http://[fda1:ffff:74de:4242::fd00]/firmware/release-candidate/sysupgrade/',
                    },
                good_signatures = 1,
                pubkeys = {
                    'ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff', -- Buildbot 
                },
            },
        },
    },
}
